export default class App {
  constructor(img, name, description, isUpdated) {
    this.img = img;
    this.name = name;
    this.description = description;
    this.isUpdated = isUpdated;
  }
}
